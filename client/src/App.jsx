

import Loading from "./components/Loading";
// The Loading component can optionally accept a prop of height (should be a valid CSS height value, ex: "100px" || "30vh")

import Users from "./components/Users";
// The Users component is expecting an array of users in the prop of "users"

import AddAUser from "./components/AddAUser";

function App() {

  return (
    <>
      <h1>Do we have users?</h1>
    </>
  );
}

export default App;
